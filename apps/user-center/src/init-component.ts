import {create, NButton, NLayout, NLayoutContent, NLayoutFooter, NLayoutSider, NMenu, NScrollbar} from "naive-ui";

const naive = create({
    components: [NButton, NLayout, NLayoutFooter, NLayoutContent, NLayoutSider,NMenu,NScrollbar,],
});

export default naive;
